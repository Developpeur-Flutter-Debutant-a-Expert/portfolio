import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFF6366F1); // Indigo
  static const primaryDark = Color(0xFF4F46E5);
  static const primaryLight = Color(0xFF818CF8);

  // Secondary colors
  static const secondary = Color(0xFF8B5CF6); // Purple
  static const secondaryDark = Color(0xFF7C3AED);
  static const secondaryLight = Color(0xFFA78BFA);

  // Accent colors
  static const accent = Color(0xFF10B981); // Green
  static const accentOrange = Color(0xFFF59E0B);
  static const accentPink = Color(0xFFEC4899);

  // Neutral colors
  static const background = Color(0xFFFAFAFA);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF3F4F6);

  // Dark theme colors
  static const backgroundDark = Color(0xFF0F172A);
  static const surfaceDark = Color(0xFF1E293B);
  static const surfaceVariantDark = Color(0xFF334155);

  // Text colors
  static const textPrimary = Color(0xFF1F2937);
  static const textSecondary = Color(0xFF6B7280);
  static const textTertiary = Color(0xFF9CA3AF);

  // Text colors for dark theme
  static const textPrimaryDark = Color(0xFFF9FAFB);
  static const textSecondaryDark = Color(0xFFD1D5DB);
  static const textTertiaryDark = Color(0xFF9CA3AF);

  // Status colors
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);

  // Level colors (for project difficulty)
  static const beginner = Color(0xFF4CAF50); // Green
  static const intermediate = Color(0xFF2196F3); // Blue
  static const advanced = Color(0xFFFF9800); // Orange
  static const expert = Color(0xFFF44336); // Red
}
